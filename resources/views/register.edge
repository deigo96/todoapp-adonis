<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  {{style('auth')}}
  {{style('https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous"')}}
  <title>Register</title>
</head>
<body>
  <form action="{{ route('register.store') }}" method="POST" class="form-signin">
    {{ csrfField() }}
    <h3 class="text-center mb-4">REGISTER</h3>
    @if(flashMessage('successMessage'))
      <div class="alert alert-success">{{flashMessage('successMessage')}} </div>
    @endif

    <div class="form-label-group mt-4">
      <input type="text" id="inputNama" class="form-control" name="username" placeholder="Nama" value="{{old('username')}}" autofocus>
      <label for="inputNama">Nama</label>
    </div>
    @if(hasErrorFor('username'))
      <div class="alert alert-danger">{{getErrorFor('username')}} </div>
    @endif
    <div class="form-label-group">
      <input type="email" id="inputEmail" class="form-control" name="email" value="{{old('email')}}" placeholder="Email address" autofocus>
      <label for="inputEmail">Email address</label>
    </div>
    @if(hasErrorFor('email'))
      <div class="alert alert-danger">{{getErrorFor('email')}} </div>
    @endif
    <div class="form-label-group">
      <input type="password" id="inputPassword" class="form-control" name="password" value="{{old('password')}}" placeholder="Password">
      <label for="inputPassword">Password</label>
    </div>
    @if(hasErrorFor('password'))
      <div class="alert alert-danger">{{getErrorFor('password')}} </div>
    @endif
    <div class="form-label-group">
      <input type="password" id="inputConfirmPassword" class="form-control" name="password_confirmation" value="{{old('confirmPassword')}}" placeholder="Confirm Password">
      <label for="inputConfirmPassword">Confirm Password</label>
    </div>
  
    <div class="checkbox mb-3">
    </div>
    <button class="btn btn-lg btn-primary btn-block" type="submit">Sign Up</button>
    <p class="text-center pt-3"><a href="{{route('login.store')}}">Login</a></p>
  </form>


  {{script('https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.slim.min.js"')}}
  {{script('https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"')}}
  {{script('https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.min.js"')}}
</body>
</html>